# EventPulse Development Notes

- Use CommonJS JavaScript and keep MVC responsibilities separated.
- Keep secrets in environment variables; never commit `.env`.
- Run `npm test` before completing changes.
- Use conventional commits: `feat:`, `fix:`, `test:`, or `docs:`.
